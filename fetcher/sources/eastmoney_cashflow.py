"""East Money cashflow: FCF data is now in eastmoney_fin (FCFF_BACK field).

This module is kept as a stub — the ZYZBAjaxNew?type=1 endpoint already provides
FCFF_BACK (free cash flow to firm, backward-looking) and MGJYXJJE (operating
cashflow per share), so a separate cashflow API call is no longer needed.
"""
